# Configuración del entorno
# Copiar este archivo como .env y ajustar valores

# Modo de datos: true = datos simulados, false = Yahoo Finance real
USE_MOCK_DATA=false

# Entorno: development, production
ENVIRONMENT=production

# Puerto del servidor (Railway lo asigna automáticamente)
PORT=8000

# CORS: dominios permitidos (separados por coma)
# Ejemplo: https://mi-frontend.vercel.app,https://otro-dominio.com
ALLOWED_ORIGINS=*

# Configuración del modelo (opcional, tienen valores por defecto)
# DIAS_PROYECCION=30
# N_SIMULACIONES=10000
# N_RUTAS_MC=500
